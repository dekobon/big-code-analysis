# bca baseline file. Generated by `bca check --write-baseline`.
# Listed offenders are filtered from threshold checks; a function that
# gets worse than its recorded value still fails. Refresh with
# `--write-baseline` when entries become stale.
#
# Path keys are relative to this file's directory (the anchor), so the
# baseline survives any `--paths` form (`.`, `src/`, `$PWD`, …).
version = 5

[provenance]
tier = "soft"
headroom = 0.95

[[entry]]
path = "big-code-analysis-cli/src/format_util.rs"
qualified = "<file>"
start_line = 1
metric = "halstead.effort"
value = 55546.053164509794

[[entry]]
path = "big-code-analysis-py/src/sarif.rs"
qualified = "collect_offenders"
start_line = 450
metric = "cognitive"
value = 24.0

[[entry]]
path = "src/preproc.rs"
qualified = "preprocess"
start_line = 325
metric = "cognitive"
value = 24.0

[[entry]]
path = "src/preproc.rs"
qualified = "record_indirect_includes"
start_line = 227
metric = "cognitive"
value = 25.0
