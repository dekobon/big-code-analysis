{{!--
  cargo-about template. Renders THIRD-PARTY-LICENSES.md shipped
  alongside every binary artefact so the Apache-2.0 §4(d) and ISC
  attribution obligations of our transitive dependencies are
  satisfied. Install paths per package format are documented in
  docs/packaging.md.
--}}
# Third-party licenses

`big-code-analysis` links against the Rust crates listed below. Each crate
retains its original copyright and license; the full text of every referenced
license appears after the crate list. This file is generated by
[`cargo-about`](https://github.com/EmbarkStudios/cargo-about) from
`about.toml` and `about.hbs` at release time.

`big-code-analysis` itself is licensed under the Mozilla Public License,
version 2.0; see the top-level `LICENSE` file (or `Cargo.toml`'s `license`
field) for the canonical declaration.

## Overview

{{#each overview}}
- **{{{name}}}** ({{count}} crate{{#if (gt count 1)}}s{{/if}})
{{/each}}

## Crates

{{#each licenses}}
### {{{name}}}

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

{{/each}}

## License texts

{{#each licenses}}
### {{{name}}}

```
{{{text}}}
```

{{/each}}
